using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TitleFire : MonoBehaviour
{
    public Vector3Int _rbgInfo;
    public Vector3Int _RedRGB;
    public Vector3Int _purpleRGB;


    // Start is called before the first frame update
    void Start()
    {
        //gameObject.GetComponent<Renderer>().
        //    material.SetColor("_TintColor", Color.HSVToRGB(255, 16, 16));
        //_rbgInfo = _RedRGB;
    }

    // Update is called once per frame
    void Update()
    {


        //gameObject.GetComponent<Renderer>().
        //    material.SetColor("_TintColor", Color.HSVToRGB(255, 16, 16, true));

    }
}
